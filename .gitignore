# Python build artefacts
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# Extracted knowledge (live data, not source)
knowledge/
final.json

# Decision-journal output — runtime artefact, not source.
# Anchored to repo root so it does NOT match the src/briar/journal/ package.
/journal/

# Anything that looks like a secret bundle
.env
.env.*
*.pem
*.key
secrets/
secrets.env
config.json
.briar/

# Editor / OS noise
.DS_Store
.idea/
.vscode/

# Real-company runbooks + planning docs — kept local, not for public repo
runbooks/
